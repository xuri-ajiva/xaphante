<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:xaphante_editor"
    xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit" x:Name="userControl" x:Class="xaphante_editor.EditorControl"
    mc:Ignorable="d"
    Height="auto" Width="auto">
    <Grid>
        <avalonedit:TextEditor Name="textEditor" Margin="0,0,0,0"
                               FontFamily="{Binding FontFamily, ElementName=userControl}"
                               FontSize="{Binding FontSize, ElementName=userControl}"
                               SyntaxHighlighting="glsl" ShowLineNumbers="True" VerticalScrollBarVisibility="Auto"
                               KeyDown="TextEditor_OnKeyDown" HorizontalScrollBarVisibility="Auto" Background="{x:Null}" Foreground="{Binding Foreground, ElementName=userControl}" LineNumbersForeground="{Binding Foreground, ElementName=userControl}" >
                    
        </avalonedit:TextEditor>

    </Grid>
</UserControl>
